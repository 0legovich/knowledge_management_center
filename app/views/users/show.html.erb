<div class="container">
  <div class="row">
    <div class="col-md-6">
      # Фото пользователя
    </div>
    <div class="col-md-6">
      <h1><%= "#{@user.name} (#{@user.role.label})" %></h1>
      <h4>Дата рождения: <%= l @user.birthday, format: :long %></h4>
      <h4>Пол: <%= @user.sex_label %></h4>
      <h4>Email: <%= @user.email %></h4>
      <%= link_to "Редактировать", edit_user_path(@user), class: "btn btn-primary" %>
    </div>
  </div>
</div>