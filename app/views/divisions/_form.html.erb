<%= simple_form_for @division do |f| %>
    <%= f.association :organization,
                      selected: get_selected_organization(params[:organization]),
                      include_blank: false%>
    <%= f.input :name %>
    <%= f.button :submit, "Создать", class: "btn btn-primary" %>
<% end %>